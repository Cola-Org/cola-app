//#mainContent.flex-box
//	c-cardBook#cardBookChannel
//		c-subView.item(c-repeat="channel in channels" c-id="'subView'+capitalize(channel.name)")
//.box(c-display="len(channels)>1")
//	c-menu#menuChannel(c-class="number2Word(len(channels)) + ' item attached blue labeled icon secondary pointing borderless'" showActivity="true")
//		a.state(c-repeat="channel in channels" c-href="channel.path" c-icon="channel.icon" c-class="channel.menuClass")
//			span(c-bind="=resource(channel.name,channel.title)")
//			div.ui.circular.label.badge.hidden(c-id="'message'+capitalize(channel.name)" style="display:none; position:absolute; top:-7px; left:65%;")
#mainContent.flex-box
	c-subView#subViewMain